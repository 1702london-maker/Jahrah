export const metadata = { title: "stays_property_detail" }

const stitchHtml = ``

export default function StaysPropertyDetailPage() {
  return <div dangerouslySetInnerHTML={{ __html: stitchHtml }} />
}
